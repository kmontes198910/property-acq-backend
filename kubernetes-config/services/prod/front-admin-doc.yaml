apiVersion: v1
kind: Service
metadata:
  name: front-web-doc-service
  namespace: medinec
spec:
  selector:
    app: front-web-doc
  ports:
    - protocol: TCP
      port: 3000
      targetPort: 3000
  type: ClusterIP
