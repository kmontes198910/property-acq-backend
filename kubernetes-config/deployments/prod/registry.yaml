apiVersion: apps/v1
kind: Deployment
metadata:
  name: registry
  namespace: property-prod
spec:
  replicas: 1
  selector:
    matchLabels:
      workload.user.cattle.io/workloadselector: apps.deployment-property-prod-registry
  strategy:
    rollingUpdate:
      maxSurge: 25%
      maxUnavailable: 25%
  template:
    metadata:
      labels:
        app: property-prod-registry
        workload.user.cattle.io/workloadselector: apps.deployment-property-prod-registry
    spec:
      containers:
        - image: kmontes2020/property-registry:v1
          imagePullPolicy: Always
          name: property-prod-registry
          ports:
            - containerPort: 8761
              protocol: TCP
      imagePullSecrets:
        - name: dokcer-hub
      restartPolicy: Always