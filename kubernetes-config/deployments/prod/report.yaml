apiVersion: apps/v1
kind: Deployment
metadata:
  name: report
  namespace: property-prod
spec:
  replicas: 1
  selector:
    matchLabels:
      workload.user.cattle.io/workloadselector: apps.deployment-property-prod-report
  strategy:
    rollingUpdate:
      maxSurge: 25%
      maxUnavailable: 25%
  template:
    metadata:
      labels:
        app: report
        workload.user.cattle.io/workloadselector: apps.deployment-property-prod-report
    spec:
      containers:
        - name: report
          image: kmontes2020/property-report:v1
          imagePullPolicy: Always
          ports:
            - containerPort: 9908
              name: http
              protocol: TCP
          envFrom:
            - secretRef:
                name: report-secret
      imagePullSecrets:
        - name: dokcer-hub
      restartPolicy: Always