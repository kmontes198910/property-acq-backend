apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: admin-ingress
  namespace: medinec-dev
spec:
  ingressClassName: nginx
  tls:
    - hosts:
        - doctorkyn-admin-dev.kynsoft.net
      secretName: medinec-tls-dev
  rules:
    - host: doctorkyn-admin-dev.kynsoft.net
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: front-admin
                port:
                  number: 3000